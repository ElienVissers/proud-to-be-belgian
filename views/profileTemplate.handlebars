<h1>Hi, {{name}}!</h1>
<h2>Please tell us a bit more about yourself before you continue to the petition.</h2>

<form method="post">
    <input name="age" placeholder="age (optional)">
    <input name="city" placeholder="city (optional)">
    <input name="homepage" placeholder="homepage (optional)">
    <input name="_csrf" type="hidden" value="{{csrfToken}}">
    <br>
    <button class="submitButton">CONTINUE</button>
</form>
